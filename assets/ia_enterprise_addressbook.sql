CREATE TABLE "ia_enterprise_addressbook" ("_id" INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL UNIQUE, "userId" INTEGER NOT NULL, "enterpriseId" INTEGER NOT NULL, "employeeName" VARCHAR(20) NOT NULL, "avatar" BLOB, "sex" SMALLINT, "nickname" VARCHAR(20), "birthday" TIMESTAMP, "department" VARCHAR(40), "approveNumber" INTEGER NOT NULL, "note" VARCHAR(200), "frequency" INTEGER NOT NULL DEFAULT 0)