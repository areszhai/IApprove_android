CREATE TABLE "ia_user_enterprise_profile" ("_id" INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL UNIQUE, "enterpriseId" INTEGER NOT NULL, "enterpriseAbbreviation" VARCHAR(40) NOT NULL, "employeeName" VARCHAR(20) NOT NULL, "avatar" BLOB, "sex" SMALLINT, "birthday" TIMESTAMP, "department" VARCHAR(40), "approveNumber" INTEGER NOT NULL, "mobilePhone" INTEGER, "officePhone" INTEGER, "email" VARCHAR(40), "note" VARCHAR(200))